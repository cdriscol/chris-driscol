{
  "name": "@chris-driscol/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cargo run --bin api",
    "build": "cargo build --release --bin api",
    "build:lambda": "bash scripts/build-lambda.sh",
    "test": "cargo test"
  }
}
